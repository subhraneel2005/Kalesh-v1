<!DOCTYPE html>
<html lang="en">


<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kalesh -v1 Welcome</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #000000;
        }

        .main {
            width: 100%;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 0 20px;
        }

        .welcome-container {
            color: #161515;
            max-width: 600px;
            padding: 20px;
            background-color: rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.3);
        }

        h1 {
            font-size: 3em;
            margin-bottom: 15px;
        }

        p {
            font-size: 1.2em;
            line-height: 1.5;
        }

        .btn {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 25px;
            font-size: 1.1em;
            color: #fff;
            background-color: #ff6b6b;
            border-radius: 25px;
            text-decoration: none;
            transition: background-color 0.3s ease;
        }

        .btn:hover {
            background-color: #ee5253;
        }
    </style>
</head>

<body>
    <div>
        <div class="welcome-container">
            <h1>Hello <strong><%= name %></strong>👋</h1>
            <p>Please click on the below link to verify your email address</p>
            <a href="<%= url %>" target="_blank">Click on this Link</a>
            <%- include('../partials/footer') %>

            <p>Or open this url in a new tab</p>
            <p><%= url %></p>
        </div>
    </div>
</body>

</html>
